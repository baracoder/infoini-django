<?xml version="1.0" encoding="UTF-8"?>
<infoini>
    <door isOpen="{% if tuer_offen %}true{% else %}false{% endif %}" />
    <cafe>
    {% for p in cafepots %}
        <pot>
            <status>{{ p.status }}</status>
            <level>{{ p.level }}</level>
        </pot>
    {% endfor %}
    </cafe>
</infoini>
